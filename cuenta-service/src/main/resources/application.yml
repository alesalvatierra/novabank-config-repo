server:
  port: 8082

spring:
  application:
    name: cuenta-service
  config:
    import: "optional:configserver:http://localhost:8888"
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/novabank_cuentas
    username: postgres
    password: "745507"
    pool:
      enabled: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
