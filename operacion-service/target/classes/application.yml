server:
  port: 8083

spring:
  application:
    name: operacion-service
  cloud:
    config:
      import-check:
        enabled: false
  datasource:
    url: jdbc:postgresql://localhost:5432/novabank_operaciones
    username: postgres
    password: "745507"
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
