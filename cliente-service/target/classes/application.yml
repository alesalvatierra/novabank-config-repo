server:
  port: 8081

spring:
  application:
    name: cliente-service
  config:
    import: "optional:configserver:http://localhost:8888"
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/novabank_clientes
    username: postgres
    password: "745507"
    pool:
      enabled: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
