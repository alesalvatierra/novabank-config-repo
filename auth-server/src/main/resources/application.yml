server:
  port: 9000

spring:
  application:
    name: auth-server
  config:
    import: "optional:configserver:http://localhost:8888"

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

#Clave para firmar los JWT
jwt:
  secret: "NovaBankSecretKeyAlejandroLopezSalvatierraRuiz1234567890"
